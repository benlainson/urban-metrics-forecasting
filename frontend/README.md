# frontend/

Shared Next.js dashboard (see `URBAN_PULSE_PROJECT_PLAN.md` Phase 5). Owned jointly — each teammate adds their domain's view (e.g. `/dashboard/transit`, `/dashboard/energy`, `/dashboard/weather`) rather than forking the app.

Scaffolded with:
```bash
npx create-next-app@latest . --typescript --tailwind --app
```
