# features/transit/

Owner: transit model teammate.

Feature engineering for ridership data: calendar features, lag/rolling features per station, joins against weather features from `features/weather/`.
