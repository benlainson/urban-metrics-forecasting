# features/energy/

Owner: energy model teammate.

Feature engineering for grid load data: calendar features, lag/rolling features per neighborhood, joins against weather features from `features/weather/`.
