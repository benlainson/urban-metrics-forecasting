# test/

Automated tests, mirroring the structure of the code they test where practical (e.g. `test/features/transit/`, `test/serving/`). Run before pushing — wire into GitHub Actions (`.github/workflows/`) once a base test suite exists.
